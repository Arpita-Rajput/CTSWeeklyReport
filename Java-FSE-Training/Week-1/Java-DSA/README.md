# Java DSA

Examples for core data structures and algorithms. Each class contains a main() demonstrating functionality.

Run examples using mvn exec plugin. For example:

mvn -q -f pom.xml compile exec:java -Dexec.mainClass="com.arpita.week1.javadsa.ArraysDemo"
