# JUnit & Mockito Example

Spring Boot project with service and repository layers, unit tests using JUnit 5 and Mockito.

Run tests:

mvn -q -f pom.xml test

Run application (example):

mvn -q -f pom.xml spring-boot:run
