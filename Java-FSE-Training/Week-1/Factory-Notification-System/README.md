# Factory Notification System

Simple factory pattern that creates notifications (Email, SMS).

Run with:

mvn -q -f pom.xml compile exec:java -Dexec.mainClass="com.arpita.week1.factorynotification.App"
